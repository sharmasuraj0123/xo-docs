---
title: Manage Your Workspace Machine
description: Monitor resources, open available interfaces, and control the remote machine behind an XO project.
icon: Manage
---

import { Step, Steps } from "fumadocs-ui/components/steps";
import { Callout } from "fumadocs-ui/components/callout";

Your project's workspace dashboard is the control center for its remote machine.
From the **Launch** tab you can monitor resources, open available interfaces,
and stop or start the machine. OpenClaw projects also expose gateway controls.

<Callout type="info">
	Controls vary by agent. Sections that mention the **OpenClaw Gateway** apply
	only to projects where that service is installed. Check the [Agent
	Compatibility Guide](/setup/compatibility) if you are unsure which setup path
	applies.
</Callout>

## What You'll Need

- An [XO account](/getting-started/create-account) with an active workspace

## Open Your Workspace

Open [beta.xo.builders](https://beta.xo.builders).

![XO Builders dashboard](/images/cfg-01-dashboard.jpeg)

Select your agent from the sidebar.

![Select your agent](/images/cfg-02-select-agent.jpeg)

If your machine is stopped, click **Start** to boot it up.

![Click Start](/images/cfg-03-click-start.jpeg)

Wait for the agent status to transition to **Ready**.

Once the machine is running, the panel shows all your workspace controls and live stats.

## Monitoring Resources

The panel displays real-time CPU and RAM usage for your workspace.

![CPU and RAM Usage](/images/cfg-04-ready.webp)

## Service Status

The panel shows the status of your services. When everything is ready, you'll see **Up** next to each service.

**OpenClaw Gateway** — When present, the gateway is running and ready to accept connections.

**Cowork API** — When present, the workspace backend is running.

![Services Up](/images/cfg-05-services-up.webp)

<Callout type="warn">
	Wait for the services required by your selected agent to show **Up** before
	opening its interface or IDE. Provisioning time varies by template.
</Callout>

## Opening Your IDE

The **Agent Connections** panel lists all the ways you can connect to your workspace.

**Desktop IDE (VS Code / Cursor)** — Click **VS Code** to open the workspace in your desktop editor.

![Click VS Code](/images/cfg-06-vscode.jpeg)

**Browser IDE (Code-Server)** — Click **code-server** to open a browser-based VS Code. No install needed.

![Click code-server](/images/cfg-07-codeserver.jpeg)

For detailed IDE setup, see the [VS Code Server guide](/getting-started/vs-code-server).

## Opening the OpenClaw Gateway

Click **OpenClaw Gateway** to open the dashboard.

![Click OpenClaw Gateway](/images/cfg-08-openclaw-gateway.jpeg)

Enter your project name in the **Gateway Token** field.

![Gateway Token field](/images/cfg-gateway-token.jpeg)

If you see "unauthorized: gateway token missing", enter the correct token — it's your exact project name (case-sensitive).

![Gateway unauthorized error](/images/cfg-gateway-unauthorized.jpeg)

## Restarting the OpenClaw Gateway

Click **OpenClaw Restart** to restart the gateway service.

![Click OpenClaw Restart](/images/cfg-09-restart-click.jpeg)

Wait for the **OpenClaw gateway restarted** confirmation.

![Gateway restart confirmed](/images/cfg-10-restart-confirmed.jpeg)

Give it a minute before opening the gateway again. For the terminal restart method (with logs), see the [Restart Gateway guide](/agents/openclaw/restart-gateway).

## Restart Your Machine

If your IDE isn't loading or your environment feels stuck, do a stop-and-restart cycle.

<Steps>
  <Step>
    ### Stop the Machine

    Click **Stop** to shut down the workspace machine. Click **Refresh** to confirm the status reads **stopped**.

  </Step>
  <Step>
    ### Start the Machine

    Click **Start** to boot the machine back up.

    ![Click Start](/images/cfg-03-click-start.jpeg)

    Wait for the status to change from **starting** to **running**.

  </Step>
  <Step>
    ### Verify Services Are Up

    Check that both **OpenClaw Gateway** and **Cowork API** show **Up** before opening the gateway or IDE.

  </Step>
</Steps>

<Callout type="info">
	If the machine stays in a **starting** state for more than a couple of
	minutes, try stopping and starting it again.
</Callout>
