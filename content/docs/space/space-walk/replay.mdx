---
title: Session Replay
description: Understand the distinction between Sessions telemetry, Timeline playback, and the separate Space Walk tool.
icon: Play
---

import { Callout } from "fumadocs-ui/components/callout";

<Lead>
  The current Space UI provides session telemetry and Git timeline playback. The separate **Space Walk** 3D replay tool is described in the bundled Wiki, but is not launched from the Sessions view.
</Lead>

## Inspect a run in Space

Open [Sessions](/docs/space/space-walk/sessions), choose its **Sessions** subview, and click a row. You can inspect token usage, tools, sub-agents, and typed prompts with reply/tool counts. There is no **Replay** button or registered `/space/#/replay` view in the current UI.

## Play workspace history

Open [Timeline](/docs/space/space-walk/timeline) and use **Play** or the scrubber to advance through mapped file dates or project commit days. This visualizes Git history; it does not replay the session's messages or individual tool calls.

## Space Walk is a separate tool

The Wiki's **Space Walk session replay** article describes a separate Go application that turns native session logs into a 3D repository map. Its server, commands, and API belong to that application. The Sessions telemetry endpoint and the Space static UI do not serve its replay API.

<Callout type="info">
  Run and configure Space Walk separately if you use it. In particular, the article's optional model-based judge can send session-derived information to the provider behind your local model CLI and consume tokens; it is separate from reading local telemetry in Space.
</Callout>
