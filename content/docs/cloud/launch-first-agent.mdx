---
title: Launch Your First Agent
description: Create an XO project from an agent template and reach a successful first interaction.
icon: Rocket
---

import { Cards, Card } from "@/components/card";
import { Step, Steps } from "fumadocs-ui/components/steps";
import { Callout } from "fumadocs-ui/components/callout";

This guide is the common **XO Cloud** path through your first launch: create an
XO project, wait for the workspace machine, and reach a first successful
interaction. Running locally instead? Use [XO Space](/docs/space).

The agent-specific quickstarts contain the exact fields, screenshots, and
authentication steps for each template.

## What You'll Need

- An [XO account](/docs/cloud/create-account)
- An agent selected using [Choose an Agent](/docs/cloud/choose-an-agent)
- The model credentials and any channel credentials listed in that agent's setup guide

## Pick the Detailed Quickstart

<Cards>
	<Card
		title="OpenClaw"
		description="Create the project, connect a model and channel, open the gateway, and test chat."
		href="/docs/agents/openclaw/setup"
	/>
	<Card
		title="Claude Code"
		description="Create the project, authenticate Claude, open code-server, and start a terminal session."
		href="/docs/agents/claude-code/setup"
	/>
	<Card
		title="Hermes"
		description="Create the project, configure a provider and channel, and open the Hermes dashboard."
		href="/docs/agents/hermes/setup"
	/>
	<Card
		title="XO Cowork"
		description="Create the project, complete in-workspace onboarding, and start your first work project."
		href="/docs/agents/xo-cowork/setup"
	/>
	<Card
		title="Antigravity"
		description="Create the project, sign in with your Google account, and start an `antigravity` terminal session."
		href="/docs/agents/antigravity/setup"
	/>
</Cards>

## The Common Launch Path

<Steps>
	<Step>
		### Create the XO Project

		Open [app.xo.builders](https://app.xo.builders), click **New Project**, and select the agent template you chose. Give the XO project a unique name and complete the template's creation form.
	</Step>
	<Step>
		### Wait for the Workspace

		XO provisions a remote machine and starts the services required by that agent. Stay on the project page and use the readiness signal named in the detailed quickstart before continuing.

		<Callout type="info">
			Do not start authentication or open an agent interface until the required services report that they are ready.
		</Callout>
	</Step>
	<Step>
		### Finish Agent Setup

		Complete any remaining model authentication, channel connection, or in-workspace onboarding in the detailed quickstart. Optional connections can be added later from the agent's setup controls.
	</Step>
	<Step>
		### Open the Agent

		Use the **Launch** area or the interface opened by the setup flow:

		- **OpenClaw:** open the OpenClaw Gateway and connect with the exact project name as the gateway token.
		- **Claude Code:** open code-server, open a terminal, and run `claude`.
		- **Hermes:** open the Hermes Dashboard and use the connected messaging app.
		- **XO Cowork:** finish onboarding and open the project chat view.
		- **Antigravity:** open code-server, open a terminal, sign in with Google, and run `antigravity`.
	</Step>
	<Step>
		### Complete a First Test

		Send a short message or start a simple coding session. A successful response confirms that the workspace, model authentication, and chosen interface are working together.
	</Step>
</Steps>

## After Your First Launch

- [Configure models, data sources, and channels](/docs/setup)
- [Learn the workspace machine controls](/docs/cloud/manage-workspace)
- [Open the browser-based IDE](/docs/cloud/vs-code-server)
- [Share the project with a collaborator](/docs/cloud/share-project)
