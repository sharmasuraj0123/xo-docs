---
title: Agents
description: Compare the documented AI agent templates available on XO Workspaces.
icon: Bot
---

import { Cards, Card } from "@/components/card";

Each agent runs in an isolated XO workspace, but the interface, setup requirements, and best-fit work differ. Use the comparison below before creating a project.

## Compare Agent Options

| Agent | Best fit | Primary interface | Initial setup |
|---|---|---|---|
| **OpenClaw** | A long-running messaging agent with skills and external integrations | OpenClaw dashboard and connected messaging apps | At least one model provider and one channel |
| **Claude Code** | Software development in a remote coding environment | Browser IDE and the `claude` terminal interface | Anthropic API key or Claude OAuth |
| **Hermes** | A messaging-first assistant with a dedicated monitoring dashboard | Hermes dashboard and connected messaging apps | Provider and channel recommended; both can be added later |
| **XO Cowork** | Chat-first work organized around persistent project files and memory | XO Cowork project view and optional channels | Provider can be added during project creation or onboarding |

If you still are not sure, follow the short [Choose an Agent](/getting-started/choose-an-agent) decision guide.

## Explore the Agents

<Cards>
	<Card
		icon={<BrandIcon name="openclaw" size={20} />}
		title="OpenClaw"
		description="Deploy a messaging agent, then add channels, models, skills, and integrations."
		href="/agents/openclaw"
	/>
	<Card
		icon={<BrandIcon name="claude-code" size={20} />}
		title="Claude Code"
		description="Run Claude Code from a browser-based editor inside an XO workspace."
		href="/agents/claude-code"
	/>
	<Card
		icon={<BrandIcon name="hermes" size={20} />}
		title="Hermes"
		description="Run a messaging-first agent and monitor it from the Hermes dashboard."
		href="/agents/hermes"
	/>
	<Card
		icon={<BrandIcon name="xo-cowork" size={20} />}
		title="XO Cowork"
		description="Work from a built-in chat and file view with persistent per-project context."
		href="/agents/xo-cowork"
	/>
</Cards>

Ready to begin? Continue to [Launch Your First Agent](/getting-started/launch-first-agent).
