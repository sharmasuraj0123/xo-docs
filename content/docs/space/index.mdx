---
title: Overview
description: What XO Space is, how it differs from XO Cloud, and how to get from install to a working local Space.
icon: Blocks
---

import { Cards, Card } from "@/components/card";
import { Callout } from "fumadocs-ui/components/callout";

<Lead>
  **XO Space** is the core open-source product and agent execution engine (`cowork-api`). It gives you real-time observability—allowing you to watch your agents work, measure their output, and manage sessions whether your agent runs **locally on your machine** or on a **managed cloud machine on XO**.
</Lead>

## Core Product Offering

Space is the unified foundation across all XO deployments:

- **Local Space (OSS)**: Install with a single `curl` command (or agent skill) on your laptop. Run agents locally while observing their thoughts, tool calls, and session logs in real time.
- **Cloud Space (Managed)**: 1-click launch on [app.xo.builders](https://app.xo.builders). Provides a managed remote machine pre-loaded with Space, browser IDE, and public endpoints.
- **Feature & API Parity**: Every agent capability, connector, and `cowork-api` endpoint is available locally and in the cloud.

## Quickstart

<Cards>
	<Card
		icon={<span className="icon-[ph--download-simple-fill] size-6" />}
		title="1. Install Space (Local OSS)"
		description="Run the single curl installer, keep terminal open, and launch local UI."
		href="/docs/space/install-space"
	/>
	<Card
		icon={<span className="icon-[ph--map-trifold-fill] size-6" />}
		title="2. Walkthrough Your First Space"
		description="Open the UI, attach an agent, and complete a verified task."
		href="/docs/space/first-space"
	/>
	<Card
		icon={<span className="icon-[ph--activity-fill] size-6" />}
		title="3. Agent Observability"
		description="Watch agent execution live and measure work delivered via quirqs."
		href="/docs/space/observability"
	/>
	<Card
		icon={<span className="icon-[ph--terminal-fill] size-6" />}
		title="4. Claude Code Skill"
		description="Install Space guidance as a native plugin for local Claude Code workflows."
		href="/docs/space/install-space-as-a-skill/claude-code"
	/>
</Cards>

## Deployment Options: Local vs. Cloud

| Feature / Aspect | **Local Space** (OSS) | **Cloud Space** (Managed) |
|---|---|---|
| **Runtime Engine** | `cowork-api` (local daemon) | `cowork-api` (cloud container) |
| **Execution Environment** | Your laptop / desktop / server | Isolated Cloud VM on [app.xo.builders](https://app.xo.builders) |
| **Setup Command** | Single `curl` script or CLI skill | 1-Click "New Project" on Web Dashboard |
| **Observability & Logs** | Local UI (`http://localhost:5002/space/`) | Cloud Dashboard & Hosted VS Code IDE |
| **Agent Support** | OpenClaw, Claude Code, Hermes, Antigravity, etc. | OpenClaw, Claude Code, Hermes, Antigravity, etc. |

<Callout type="info">
	Whether launched locally or on the cloud, every agent runs inside a **Space**. Both environments expose the identical `cowork-api` endpoints. Terms: [Glossary](/docs/glossary).
</Callout>

## Next

[Install Space](/docs/space/install-space)
